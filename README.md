# Number_-Fact_-Finder_Workspace
Number Fact Finder Workspace is a Dart-based project that retrieves interesting facts about numbers using an API. It uses a command-line interface (CLI) to allow users to enter numbers and view their corresponding facts. The project demonstrates API integration, JSON processing, asynchronous programming, and modular package organization.
